import { ScreenBackground, Terminal } from "@components";

export const HomePageDesktop = () => {
  return (
    <main>
      <ScreenBackground>
        <Terminal />
      </ScreenBackground>
    </main>
  );
};
