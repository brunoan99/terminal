import { NotFoundTerminal, ScreenBackground } from "@components"

export const NotFoundPage = () => {
  return (
    <main>
      <ScreenBackground>
        <NotFoundTerminal />
      </ScreenBackground>
    </main>
  )
}
