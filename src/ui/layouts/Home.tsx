import { ScreenBackground, Terminal } from "@components";

const HomePage = () => {
  return (
    <main>
      <ScreenBackground>
        <Terminal />
      </ScreenBackground>
    </main>
  );
};

export { HomePage };
